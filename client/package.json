{
  "name": "client",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --filter './apps/*' dev",
    "build": "pnpm -r --filter './packages/*' build && pnpm -r --filter './apps/*' build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}