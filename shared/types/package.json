{
    "name": "@shared/types",
    "version": "1.0.0",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "scripts": {
        "clean": "rm -rf dist tsconfig.tsbuildinfo",
        "build": "pnpm clean && tsc",
        "lint": "eslint src/**/*.ts",
        "dev": "tsc -w"
    },
    "dependencies": {
        "zod": "^4.3.5"
    },
    "devDependencies": {
        "typescript": "^5.9.3"
    }
}